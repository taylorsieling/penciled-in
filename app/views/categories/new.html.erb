<h1>Create an Event Categoy</h1>

<%= render partial: 'layouts/errors', locals: {object: @category} %>

<%= form_for(@category) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

<%= f.submit %>
<% end %>